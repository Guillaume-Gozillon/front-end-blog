export const API_URL = 'http://localhost:1337'

export const API_POSTS = `${API_URL}/posts/`

export const URL_COMMENTS = `${API_URL}/comments/`

export const API_LOGIN = `${API_URL}/auth/local`

export const API_REGISTER = `${API_URL}/auth/local/register`
